package in.reqres.endpoints;

public class Endpoints {
    public static final String
            booksEndpoint = "/BookStore/v1/Books",
            bookEndpoint = "/BookStore/v1/Book",
            usersEndpoint = "/users";

    public String usersEndpointId = "/users/{id}";
}
